import { api } from '../store/useAuthStore';

/**
 * Standard API client for the application.
 * Re-exported from useAuthStore to provide a centralized access point
 * and satisfy common import patterns across the codebase.
 */
export default api;
